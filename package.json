{
  "name": "ecs-pf",
  "version": "2.2.27",
  "description": "CLI for port-forwarding to RDS via AWS ECS",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ecs-pf": "dist/index.js"
  },
  "files": [
    "dist/**/*",
    "README.md",
    "CHANGELOG.md"
  ],
  "keywords": [
    "aws",
    "ecs",
    "rds",
    "port-forwarding",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "scripts": {
    "connect": "bun run build && bun dist/index.js connect",
    "exec": "bun run build && bun dist/index.js exec",
    "enable-exec": "bun run build && bun dist/index.js enable-exec",
    "build": "bun run generate-version && bunx decopin build",
    "generate-version": "bun scripts/generate-version.mjs",
    "generate-changelog": "bun scripts/generate-changelog.mjs",
    "changelog:init": "bun scripts/generate-changelog.mjs init",
    "changelog:update": "bun scripts/generate-changelog.mjs generate",
    "start": "bun dist/index.js connect",
    "type-check": "tsc --noEmit",
    "test": "bun run type-check && bun run build && bun test",
    "test:unit": "bun test test/unit",
    "test:integration": "bun test test/integration",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "prepublishOnly": "bun run check",
    "format": "biome format --write",
    "format:check": "biome format",
    "lint": "biome lint --write",
    "lint:check": "biome lint",
    "check": "biome check --write",
    "check:dry": "biome check",
    "ci": "biome ci"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.5",
    "@types/bun": "^1.3.2",
    "@types/inquirer": "^9.0.8",
    "conventional-changelog-cli": "^5.0.0",
    "conventional-changelog-conventionalcommits": "^9.0.0",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.830.0",
    "@aws-sdk/client-ecs": "^3.830.0",
    "@aws-sdk/client-rds": "^3.830.0",
    "@aws-sdk/client-ssm": "^3.830.0",
    "@inquirer/prompts": "^7.5.3",
    "chalk": "^5.4.1",
    "decopin-cli": "^2026.902.643",
    "fuse.js": "^7.1.0",
    "inquirer": "^13.2.4",
    "inquirer-search-list": "^1.2.6",
    "remeda": "^2.23.1",
    "valibot": "^1.1.0"
  }
}
